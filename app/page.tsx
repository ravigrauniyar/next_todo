"use client";

import TodoList from "@/todos/page";

/**
 * Home: Represents the home page component of the application.
 *
 * Returns:
 * - A TodoList component to be rendered on the home page.
 */
export default function Home() {
  return <TodoList />;
}
